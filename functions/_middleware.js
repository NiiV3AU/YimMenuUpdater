export async function onRequest(context){const response=await context.next();const url=new URL(context.request.url);if(url.pathname!=='/'){return response;}const acceptLanguage=context.request.headers.get('accept-language')||'en';if(acceptLanguage.startsWith('de')){return Response.redirect(new URL('/de/',url),302)}else if(acceptLanguage.startsWith('fr')){return Response.redirect(new URL('/fr/',url),302)}return response}